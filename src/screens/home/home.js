import React from 'react';
import { Text, SafeAreaView, StyleSheet } from 'react-native';

export const Home = () => {
  return (
    <SafeAreaView>
      <Text>Page contet</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
